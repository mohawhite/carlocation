// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com"], // Si vous utilisez des images d'Unsplash
    },
};

module.exports = nextConfig;